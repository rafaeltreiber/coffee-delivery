import { Box, Typography } from "@mui/material";
import styled from "styled-components";

export const Container = styled(Box)`
  width: 1440px;
  height: 1645px;

  padding: 0 160px;
`;

export const OurCoffees = styled(Typography)`
  font-family: "Baloo 2", sans-serif;
  font-weight: 800;
  font-size: 32px;
`;
